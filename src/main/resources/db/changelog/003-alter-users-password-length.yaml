databaseChangeLog:
  - changeSet:
      id: 003-alter-users-password-length
      author: Agshin Nadirov
      changes:
        - modifyDataType:
            tableName: users
            columnName: password
            newDataType: varchar(256)

        - modifyDataType:
            tableName: users
            columnName: password
            newDataType: varchar(256)

      rollback:
        - modifyDataType:
            tableName: users
            columnName: password
            newDataType: varchar(50)
